# Application
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth.js Configuration
# Generate a secret: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-change-this

# Trust the host for Auth.js (required for production behind proxies)
AUTH_TRUST_HOST=true

# ============================================
# OAuth Provider Configuration
# ============================================

# Google OAuth
# Create credentials at: https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID=your-google-client-id
AUTH_GOOGLE_SECRET=your-google-client-secret

# Facebook OAuth
# Create app at: https://developers.facebook.com/apps
AUTH_FACEBOOK_ID=your-facebook-app-id
AUTH_FACEBOOK_SECRET=your-facebook-app-secret

# Apple OAuth
# Create credentials at: https://developer.apple.com/account/resources/identifiers
AUTH_APPLE_ID=your-apple-service-id
AUTH_APPLE_SECRET=your-apple-secret-key

# ============================================
# OAuth Setup Instructions
# ============================================

# GOOGLE SETUP:
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create a new project or select an existing one
# 3. Click "Create Credentials" -> "OAuth client ID"
# 4. Choose "Web application"
# 5. Add authorized JavaScript origins:
#    - http://localhost:3000 (development)
#    - https://yourdomain.com (production)
# 6. Add authorized redirect URIs:
#    - http://localhost:3000/api/auth/callback/google (development)
#    - https://yourdomain.com/api/auth/callback/google (production)

# FACEBOOK SETUP:
# 1. Go to https://developers.facebook.com/apps
# 2. Create a new app (choose "Consumer" or "None")
# 3. Add "Facebook Login" product
# 4. In Settings > Basic, get your App ID and App Secret
# 5. In Facebook Login > Settings, add valid OAuth redirect URIs:
#    - http://localhost:3000/api/auth/callback/facebook (development)
#    - https://yourdomain.com/api/auth/callback/facebook (production)

# APPLE SETUP:
# 1. Go to https://developer.apple.com/account/resources/identifiers
# 2. Create an App ID (if you don't have one)
# 3. Create a Services ID for "Sign in with Apple"
# 4. Configure the Services ID with your domain and redirect URLs:
#    - http://localhost:3000/api/auth/callback/apple (development)
#    - https://yourdomain.com/api/auth/callback/apple (production)
# 5. Create a Key for "Sign in with Apple" and download it
# 6. Generate the client secret using the key (requires additional setup)
#    See: https://authjs.dev/reference/core/providers/apple

# ============================================
# AWS S3 Configuration (for file uploads)
# ============================================
AWS_REGION=eu-north-1
AWS_S3_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# ============================================
# Stripe Configuration (for payments)
# ============================================
# Get your keys at: https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key

# STRIPE SETUP:
# 1. Go to https://dashboard.stripe.com/apikeys
# 2. Copy your "Publishable key" (starts with pk_test_ or pk_live_)
# 3. Copy your "Secret key" (starts with sk_test_ or sk_live_)
# 4. For webhooks (production), create a webhook endpoint at:
#    https://dashboard.stripe.com/webhooks
#    - Endpoint URL: https://yourdomain.com/api/payments/webhook
#    - Events to listen for: checkout.session.completed, customer.subscription.deleted, invoice.payment_failed

